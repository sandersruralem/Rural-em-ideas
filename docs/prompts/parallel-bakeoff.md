# Prompt: Parallel approach bake-off (3 Cloud Agents)

Use the same spike brief; launch three agents with different approach lines.

---

Shared instructions: use `docs/prompts/spike-agent.md` with spike `{{SPIKE_PATH}}`.

### Agent 1 — Print / briefing first
Approach override: Optimize for a printable one-pager an EM can hand elected officials. Interactive UI is secondary.

### Agent 2 — Phone field first
Approach override: Optimize for one-handed phone use in a vehicle with spotty data. Defer desktop polish.

### Agent 3 — CSV / Excel bridge first
Approach override: Optimize for import/export that lands cleanly in Excel/Sheets already used by the office. Minimal custom persistence.

After all three PRs land, review demos only and pick one line of attack to promote.
